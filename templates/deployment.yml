---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-server
  namespace: {{ .Values.namespace }}
  labels:
    app: ftp-server
    env: {{ .Values.environment }}
    managed-by: Helm
    version: {{ .Chart.Version }}
spec:
  selector:
    matchLabels:
      app: ftp-server
  replicas: {{ .Values.replicaCount | default 1 }}
  template:
    metadata:
      labels:
        app: ftp-server
        env: {{ .Values.environment }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: {{ .Values.image.repository }}
          imagePullPolicy: {{ .Values.imagePullPolicy }}
          ports:
            - containerPort: {{ .Values.service.ftp.controlPort }}
              name: ftp
            - containerPort: {{ .Values.service.ftp.pasvMinPort }}
              name: pasvmin
            - containerPort: {{ .Values.service.ftp.pasvMaxPort }}
              name: pasvmax
            - containerPort: {{ .Values.service.sftp.port }}
              name: sftp    
          env:
            - name: USER_CONFIG_PATH
              value: {{ .Values.userConfigPath | quote }}
            - name: PASV_MIN_PORT
              value: {{ .Values.service.ftp.pasvMinPort | quote }}
            - name: PASV_MAX_PORT
              value: {{ .Values.service.ftp.pasvMaxPort | quote }}
            - name: PASV_ADDRESS
              value: {{ .Values.service.ftp.pasvAddress | default "127.0.0.1"}}
          volumeMounts:
            - name: user-config
              mountPath: /etc/vsftpd/users.json
              subPath: users.json
              readOnly: true
            - name: ftp-storage
              mountPath: /data
          resources:
            limits:
              memory: {{ .Values.resources.limits.memory }}
            requests:
              memory: {{ .Values.resources.requests.memory }}
          securityContext:
            runAsNonRoot: false
            readOnlyRootFilesystem: {{ .Values.securityContext.readOnlyRootFilesystem | default false }}
            allowPrivilegeEscalation: {{ .Values.securityContext.allowPrivilegeEscalation }}
            seccompProfile:
              type: {{ .Values.securityContext.seccompProfile.type }}
      volumes:
        - name: user-config
          configMap:
            name: {{ .Release.Name }}-users-config
        - name: ftp-storage
          persistentVolumeClaim:
            claimName: {{ .Release.Name }}-pvc